class HallDetailModel {
  constructor({ hallName, capacity, location }) {
    this.hallName = hallName;
    this.capacity = capacity;
    this.location = location;
  }
}

export default HallDetailModel;
